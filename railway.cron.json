{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "python backend/refresh_stale.py --days 5 --limit 50",
    "cronSchedule": "0 7 * * *",
    "restartPolicyType": "NEVER"
  }
}
