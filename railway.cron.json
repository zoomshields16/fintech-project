{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "python backend/refresh_stale.py --days 6 --limit 20",
    "cronSchedule": "0 7 * * *",
    "restartPolicyType": "NEVER"
  }
}
